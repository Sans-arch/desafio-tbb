import styled from 'styled-components';

export default styled.input`
  border: none;
  height: 50px;
  width: 100%;
  padding: 16px;
  outline: none;
  font-size: 16px;
  border-radius: 8px;

  &::placeholder {
    color: #BCBCBC;
  }

`;
